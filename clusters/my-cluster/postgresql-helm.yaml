apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: postgresql
  namespace: default
spec:
  prune: true
  interval: 1m
  chart:
    spec:
      chartRef:
      kind: OCIRepository
      name: postgresql
      namespace: default
      interval: 10m
  values:
    